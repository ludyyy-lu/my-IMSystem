syntax = "proto3";

// 这对吗
package chat;
option go_package = "./chat;chat";

message ChatMessage {
  int64 from_user_id = 1;
  int64 to_user_id = 2;
  string content = 3;
  int64 timestamp = 4;
  string message_id = 5; // 可选，用于去重/ack
}
